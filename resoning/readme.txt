In this section,firstly,we load the data in n-Triples form into Apache-jena,then we use the ontology file to establish a graph database,lastly,we write rules to combine the information of what the genre of the movie and who act in this movie to realize the function,the function enable us to know what kinds of movie has an actor acted.
The files in tdb is the graph database.
rules.ttl is the rules.
example.PNG shows a instance of finding 10 actors who has acted in suspense movie.